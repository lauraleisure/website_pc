/*左侧菜单栏事件绑定*/
var menuDropDown=function () {

    var methods={
        init:function () {
            
        }
    };
    return methods;
};

(function () {
    var ctrl = menuDropDown();
    //将ctrl对象挂到window上，供页面上使用。
    window._ctrl=ctrl;
})();